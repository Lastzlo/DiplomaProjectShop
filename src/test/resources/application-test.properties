#postgres
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost/shop2test}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:12345}
spring.jpa.generate-ddl=true

#port
server.port=2021

#file size limit
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.max-file-size=10MB

#testPictures
testPicture.location=src/test/resources/forProductController/pictures/test.jpg

#AWS properties add only when test
